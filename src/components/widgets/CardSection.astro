
---
const { cards } = Astro.props;
---
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Card Flip Component</title>
    <style>
      /* Global styles */
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        background-color: #f4f4f4;
      }

      .container {
        max-width: 1500px;
        margin: 0 auto;
        padding: 20px;
        text-align: center;
      }

      .header {
        margin-bottom: 40px;
      }

      .header h1 {
        font-size: 40px;
        margin-bottom: 3px;
      }

      .header p {
        font-size: 18px;
        color: #555;
      }

      /* Card Container */
      .card-container {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 20px;
        justify-items: center;
      }

      /* Individual Card Styling */
      .card {
        width: 100%;
        max-width: 300px;
        height: 400px;
        perspective: 1000px; /* Ensures 3D effect for flip */
      }

      .card-inner {
        width: 100%;
        height: 100%;
        position: relative;
        transform-style: preserve-3d;
        transition: transform 0.5s ease;
      }

      .card:hover .card-inner {
        transform: rotateY(180deg); /* Flip the card */
      }

      .card-front,
      .card-back {
        position: absolute;
        width: 100%;
        height: 100%;
        backface-visibility: hidden; /* Hides the back side when flipped */
        border-radius: 10px;
      }

      .card-front img {
        width: 100%;
        height: 100%;
        object-fit: contain;
        border-radius: 10px;
      }

      .card-back {
        background-color: #b2161e;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 20px;
        color: #fff;
        font-size: 18px;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        transform: rotateY(180deg); /* Back side starts flipped */
        white-space: pre-line;
      }

      /* Styling for card content */
      .card-back .title {
        font-weight: bold; /* Make the title bold */
        font-size: 30px; /* Larger title font size */
        position: absolute; /* Position relative to the parent container */
        top: 10px; /* Position it 10px from the top */
        left: 10px; /* Position it 10px from the left */
      }
      .card-back .description {
        font-size: 18px; /* Smaller description font size */
        text-align: left;
      }
      .card-back .image {
        width: 100%; /* Ensure the image fits within the card */
        height: auto;
        max-height: 150px; /* Optional: Limit the size of the image */
        margin-top: 20px; /* Add some space above the image */
        object-fit: cover; /* Ensure the image covers the space */
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="header">
        <h1 class="font-bold">Otras formas de hacer tu donación</h1>
      </div>

      <div class="card-container">
        {cards.map(card => (
          <div class="card">
            <div class="card-inner">
              <div class="card-front">
                <img src={card.image} alt={card.title} />
              </div>
              <div class="card-back">
                <div class="title">{card.title}</div> 
                <div class="description">{card.content}</div> 
                <img src={card.backImage} class="image" />
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </body>
</html>